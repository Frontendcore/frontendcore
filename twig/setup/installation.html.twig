{% set current_children = "Installation" %}
{% set current_section = "Setup" %}
{% set relative_path = "../" %}

{% extends "../_layout/_basic.html.twig" %}

{% block main %}
    <h1>Frontendcore Installation</h1>
    <p>Congratulations! You've choose the best option to work with frontendcore, just follow the following steps to install.</p>
    <article class="box-background">
        <h2>1. Install NodeJs v0.10.36</h2>
        <p>Nodejs is required for the compilation and to execute the grunt tasks, so please <a href="http://blog.nodejs.org/2015/01/26/node-v0-10-36-stable/" target="_blank">download & install NodeJS</a> from the official site.</p>
        <p class="msg-info"> The reason to install v.0.10.36 is because <a href="https://github.com/sindresorhus/grunt-sass/issues/203">a bug of libsass with latest versions of node.</a></p>
    </article>
    <article class="box-background">
        <h2>2. Install Yeoman</h2>
        <p><a href="http://yeoman.io" target="_blank">Yeoman</a> is required for the generator-frontendcore (next step) and it's necessary just to create the enviroment, once it's created frontendcore doesn't require Yeoman anymore.</p>
        <code data-fc-modules="code" class="d-ib mt-s mb-l va-m">
            npm install -g yo
        </code>
    </article>
    <article class="box-background">
        <h2>3. Install Generator Frontendcore</h2>
        <p>You need to install this in your computer just to create the work enviroment, once it's created frontendcore doesn't require the Generator frontendcore anymore.</p>
        <code data-fc-modules="code" class="d-ib mt-s mb-l va-m">
            npm install -g generator-frontendcore
        </code>
    </article>
    <article class="box-background">
        <h2>4. Install Frontendcore</h2>
        <p>Finally you are ready to install frontendcore and create the workspace in your project. First go to the folder you want to install frontendcore and execute the generator.</p>
        <code data-fc-modules="code" class="d-ib mt-s mb-l va-m">
            yo frontendcore
        </code>
    </article>
    <article class="box-background">
        <h2>5. Check that installation is ok</h2>
        <p>First of all check that you've got the following folders in your project:</p>
        <pre><code data-fc-modules="code" class="d-ib mt-s mb-n pa-l">components/
css/
fonts/
js/
.bowerrc
bower.json
Gruntfile.js
package.json
index.html</code></pre>
        <p>Then execute the grunt task to compile all the code, if everything is fine it should ends without errors.</p>
        <code data-fc-modules="code" class="d-ib mt-s mb-l va-m">
            grunt
        </code>
    </article>
    <article class="box-background">
        <h2>Frequently problems & questions about the installation</h2>
        <ul class="list">
            <li>
                <a href="#question-1" data-fc-modules="toggle">Do I need Admin permissions to install FrontendCore?</a>
                <p id="question-1" class="box pa-s hidden">Yes you will need, so if you are not Admin of your computer remember to open a terminal as Admin or add "Sudo" to the commands.</p>
            </li>
            <li>
                <a href="#question-1" data-fc-modules="toggle">After execute the "yo frontendcore" command I've got a permissions error, What should I do?</a>
                <p id="question-1" class="box pa-s hidden">Probably everything comes well except the "npm install" command, so just execute the npm install as admin or add the "sudo" to the command: "sudo npm install".</p>
            </li>
        </ul>
    </article>
    <a href="css.html" class="icon-arrow-right fz-l"> <strong>Setup the CSS</strong></a>
{% endblock %}