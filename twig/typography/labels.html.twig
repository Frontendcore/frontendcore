{% set current_children = "Labels" %}
{% set current_subtitle = "Titles, paragraphs and more font styles" %}

{% set current_section = "Typography" %}
{% set relative_path = "../" %}
{% import "../_macros/_demo.html.twig" as demos %}

{% extends "../_layout/_basic.html.twig" %}

{% block main %}
    <h1><span class="label _css fz-xl va-m">CSS</span> Labels</h1>
    <section class="box-lighten">
        <h2>Default Labels</h2>
        {{ demos.demo(
            'labels-default',
            PathComponents ~ '/labels/html/labels-default.html'
        )}}

    </section>
    <section class="box-lighten">
        <h2>Labels with fz modifiers</h2>
        <p>You can use the fz modifiers to change the size of the label</p>
        {{ demos.demo(
            'labels-fz',
            PathComponents ~ '/labels/html/labels-fz.html'
        )}}
    </section>

{% endblock %}