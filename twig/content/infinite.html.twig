{% set current_children = "Infinite Scroll" %}
{% set current_section = "Content" %}
{% set current_subtitle = "Infinite scroll has been called autopagerize, unpaginate, endless pages. But essentially it is pre-fetching content from a subsequent page and adding it directly to the user’s current page." %}
{% set relative_path = "../" %}
{% import "../_macros/_demo.html.twig" as demos %}

{% extends "../_layout/_basic.html.twig" %}

{% block main %}
        {{ demos.demo(
            'carousel-div',
            PathComponents ~ 'infinite/html/index1.html'
        )}}

    {{ demos.settings(
    {
        "1" : {
            "name" :        "data-fc-item-selector",
            "description" : "Mandatory: Selector for all items you'll retrieve",
        },
        "2" : {
            "name" :        "data-fc-next-selector",
            "description" : "Mandatory: Selector for the NEXT link (to page 2)",
        },
        "3" : {
            "name" :        "data-fc-buffer-px",
            "description" : "Increase this number if you want infinite scroll to fire quicker (a high number means a user will not see the loading message).",
            "default" : "40"
        },
    }
    ) }}
{% endblock %}