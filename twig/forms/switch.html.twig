{% set current_children = "Switch" %}
{% set current_subtitle = "Turn a checkbox into a switch" %}
{% set current_section = "Forms" %}
{% set relative_path = "../" %}
{% import "../_macros/_demo.html.twig" as demos %}

{% extends "../_layout/_basic.html.twig" %}

{% block main %}
    {{ demos.demo(
        'switch',
        '../../components/switch/html/switch.html'
    )}}
    {{ demos.settings(
        {
            "1" : {
                "name" :        "data-on",
                "description" : "text to show when the switch is on",
                "default" :     ""
            },
            "2" : {
                "name" :        "data-off",
                "description" : "text to show when the switch is off",
                "default" :     ""
            }
        }
    )}}
{% endblock %}