{% set current_children = "Password Meter" %}
{% set current_section = "Forms" %}
{% set relative_path = "../" %}
{% import "../_macros/_demo.html.twig" as demos %}

{% extends "../_layout/_basic.html.twig" %}

{% block main %}
    <h3>Password meter</h3>
    {{ demos.demo(
        'simple-range',
        PathComponents ~ 'form-password-meter/html/simple-password.html',
        false,false,true
    )}}


    {{ demos.settings(
    {
        "1" : {
            "name" :        "data-fc-target-input",
            "description" : "Targets and input (jquery selector) to display the value of the range",
            "default": "false"
        }
    }
    )}}
{% endblock %}
