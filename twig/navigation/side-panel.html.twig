{% set current_children = "Side Panel" %}
{% set current_section = "Navigation" %}
{% set relative_path = "../" %}
{% import "../_macros/_demo.html.twig" as demos %}

{% extends "../_layout/_basic.html.twig" %}

{% block main %}
    <p>Inside this panel you can add the code you want, you can use it to show some info, add a menu or an image gallery for example.</p>
    <section class="box-lighten">
        <h2>Side Panel on the left</h2>
        {{ demos.demo(
            'side-panel-1',
            PathComponents ~ 'side-panel/html/side-panel-left.html'
        )}}
    </section>
    <section class="box-lighten">
        <h2>Side panel on the right</h2>
        {{ demos.demo(
            'side-panel-2',
            PathComponents ~ 'side-panel/html/side-panel-right.html'
        )}}
    </section>
    <section class="box-lighten">
        <h2>Side panel tabbed</h2>
        <p>If you include the link inside sidepanel it will be shown as a vertical tab. If you want to personalize the appearance just use the data-fc-class parameter.</p>
        {{ demos.demo(
        'side-panel-tab',
        PathComponents ~ 'side-panel/html/side-panel-tab.html'
        )}}
    </section>
    {{ demos.settings(
        {
            "href" : {
                "name" :        "href",
                "description" : "The href of the link will be an anchor to the menu."
            },
            "data-fc-position" : {
                "name" :        "data-fc-position",
                "description" : "Define the left or right position for the panel.",
                "default" :     "left"
            },
            "data-fc-width" : {
                "name" :        "data-fc-width",
                "description" : "Define the width for the panel.",
                "default" :     "15.6em"
            },
            "data-fc-tab-top" : {
                "name" :        "data-fc-tab",
                "description" : "If it's set to true will move the open link to a side tab on the side you specified in position",
                "default" :     "false"
            },
            "data-fc-tab-top" : {
                "name" :        "data-fc-tab-top",
                "description" : "Defines the top position of the tab for the side panel tabbed",
                "default" :     "50%"
            },
            "data-fc-clone" : {
                "name" :        "data-fc-clone",
                "description" : "If you set this to true the content inside the panel should be a clone of the original. This is very useful to create sidemenus using the same menu of desktop, because the copy ignores the desktop, mobile and tablet classes.",
                "default" :     "false"
            }
        }
    )}}

{% endblock %}