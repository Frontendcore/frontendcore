{% set current_children = "Loading" %}
{% set current_section = "Callouts & Prompts" %}
{% set relative_path = "../" %}
{% import "../_macros/_demo.html.twig" as demos %}

{% extends "../_layout/_basic.html.twig" %}

{% block main %}
    <h1><span class="label-css fz-xl va-m">CSS</span> <span class="label-js fz-xl va-m">JS</span> Loading</h1>
    <section class="box-lighten">
        <h2>Simple Loading</h2>
        <p>Turns any div, section, article, p, a, span, strong, i or b into a loading object overriding and hiding all the css. All the JS components automatically removes the class 'loading' when are initialized</p>
        <p class="mb-n">You can turn remove the loading class of any object calling the following FrontendTool:</p>
        <pre class="mb-n">
            <code data-fc-modules="code" class="mb-n">FrontendTools.removeLoading(Target,classNamesToAdd, callback);</code>
        </pre>
        {{ demos.demo(
            'loading-simple',
            '../_demos/callouts/loading-simple.html'
        )}}
    </section>
{% endblock %}